apply plugin: 'com.android.application'

android {
    compileSdkVersion 26 // 23
    // "23.0.3"

    defaultConfig {
        applicationId "android.test.concurrent"
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'tools.android:utils:1.1.6'
}
